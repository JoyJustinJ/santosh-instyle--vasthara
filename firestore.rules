rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allows anybody to read schemes, but let's allow the admin to write for now
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
